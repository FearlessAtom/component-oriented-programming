const gameSettings = {
    minCardCount: 4,
    maxCardCount: 30,

    minCardsToMatch: 2,
    maxGridColumnCount: 6,
}

const defaultGameSettings = {
    cardCount: 30,
    moveLimit: 30,
    cardsToMatch: 2,
}

export { gameSettings, defaultGameSettings };
